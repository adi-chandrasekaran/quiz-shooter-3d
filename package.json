{
  "name": "quiz-shooter-3d",
  "version": "0.1.0",
  "private": true,
  "description": "Multiplayer 3D quiz shooter prototype built for Cursor.",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w server\" \"npm run dev -w client\"",
    "dev:client": "npm run dev -w client",
    "dev:server": "npm run dev -w server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
